const multiplicationTable = (num) => {
  let result = [];
  for (let i = 1; i <=10; i++){
   let res= result.push = i * num;
    console.log(`${i}*${num}=${res}`);
  }
 
}
multiplicationTable(6);
multiplicationTable(5);

let num = 15;
for (let i = 1; i <=10; i++){
   let r=i * num;
    console.log(`${i}*${num}=${r}`);
  }
